import React, {Component} from "react";

class ClassComponent extends Component {
    render() {  // render method to return HTML in the class component
        return <h1>Hello, class components</h1>
    }
}

export default ClassComponent;